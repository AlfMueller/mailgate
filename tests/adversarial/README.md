# Adversarial Tests (planned)

This directory will contain synthetic prompt-injection, forged-header, MIME parser, Unicode, authorization, and data-exfiltration test cases.

Fixtures must be safe to publish and must never contain live secrets or personal data.
