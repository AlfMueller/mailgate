# Mail Worker (planned)

This directory is reserved for future mailbox ingestion, authentication evaluation, MIME sanitization, classification, and deterministic policy processing.

The worker must not expose mailbox credentials to classifiers or Hermes and must fail closed when processing is incomplete or invalid.
